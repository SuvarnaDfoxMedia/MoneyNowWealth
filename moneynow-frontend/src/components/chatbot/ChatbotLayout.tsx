"use client";

// Chatbot is temporarily disabled across the project to avoid serving inactive assistant flows.
// Previous full chatbot UI/logic has been intentionally commented out instead of removed.
/*
import { FormEvent, useEffect, useRef, useState } from "react";
import {
  Minus,
  Maximize2,
  SendHorizontal,
  RotateCcw,
  MessageSquare,
  X,
} from "lucide-react";

...existing chatbot implementation intentionally disabled...
*/

export default function ChatbotLayout() {
  // Chatbot is intentionally disabled for now.
  return null;
}

